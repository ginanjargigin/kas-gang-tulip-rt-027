const{sign}=require("./_auth");module.exports=async(req,res)=>{if(req.method!=="POST")return res.status(405).json({error:"Method not allowed"});let{email,password}=req.body||{};if(!process.env.ADMIN_EMAIL||!process.env.ADMIN_PASSWORD||!process.env.APP_SECRET)return res.status(500).json({error:"Environment Vercel belum lengkap."});if(email!==process.env.ADMIN_EMAIL||password!==process.env.ADMIN_PASSWORD)return res.status(401).json({error:"Email atau password salah."});res.json({token:sign({sub:email,exp:Date.now()+8*60*60*1000},process.env.APP_SECRET)})};